# Expo/React Native は Metro が pnpm のネスト node_modules を解決できないため平坦化する。
node-linker=hoisted
# 注意: 親に pnpm-workspace.yaml があるため、mobile では必ず
#   pnpm install --ignore-workspace
# を使う（独立プロジェクト扱い。Web版を撤去しルート昇格したら不要になる）。
